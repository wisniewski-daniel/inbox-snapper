{
  "name": "inbox-snapper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "lint": "node scripts/lint.mjs",
    "build": "node scripts/build.mjs",
    "gates": "node scripts/run-gates.mjs"
  }
}
